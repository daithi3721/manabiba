<section id="Intro"  >
  <div class="container">
    <div class="row" >
      <div class="col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1 col-sm-12">
        <div class="help">
          <br>
          <h3 style="text-align: center">基礎物理</h3><br>
          <h3 style="text-align: center"><strong> 講師:吉田光輝 全48講座</strong></h3><br>
          <%= image_tag('buturi_hover.png', :class => "img-responsive") %>
        </div><br>
        <% if @basis_lessons.present? %>
          <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover  scrollclass">
              <thead>
                <tr>
                  <th>No.</th>
                  <th>title</th>
                  <th>action</th>
                </tr>
              </thead>
              <% @basis_lessons.each do |basis_lesson| %>
              <tbody>
                <tr>
                  <td><%= basis_lesson.number %></td>
                  <td><%= basis_lesson.name %></td>
                  <td><%= link_to "授業を見る", basis_lesson %></a></td>
                </tr>
              </tbody>
              <% end %>
            </table>
          </div>
        <% else %>
          <p style="text-align: center">基礎講座情報がありません。</p>
        <% end %>
      </div>
    </div>
  </div>
</section>